name:                princesa
version:             0.1.0.0
github:              "githubuser/princesa"
license:             BSD3
author:              "Franco Testagrossa"
maintainer:          "franco.testagrossa@gmail.com"
copyright:           "2020 Franco Testagrossa here"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/princesa#readme>

dependencies:
- base >= 4.7 && < 5
- aeson
- servant-server
- wai
- warp

library:
  source-dirs: src
  exposed-modules:
    Lib
    Infrastructure.Candidates
    Infrastructure.Jobs
    Infrastructure.HttpServer
  other-modules:
    Modules.Candidates.Domain
    Modules.Candidates.Infrastructure
    Modules.Candidates.Service
    Modules.Jobs.Domain
    Modules.Jobs.Infrastructure
    Modules.Jobs.Service


executables:
  princesa-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - base
    - princesa

tests:
  princesa-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - base
    - princesa
    - hspec
    - hspec-wai
    - hspec-wai-json
    - aeson
